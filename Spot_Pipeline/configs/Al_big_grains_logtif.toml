# Al big grains — LoG TIF run (ESRF May 2026)
# Reproduces output/test_logtif_full.h5

seg_path         = "/workspaces/Data-Inspection-Container/DATA/ESRF_May_2026/Al_big_grains/im_seg"
seg_type         = "log_tif"
registry         = "/workspaces/Data-Inspection-Container/configs/scan_registry.toml"
scan_name        = "Al_big_grains"
raw_dir          = "/workspaces/Data-Inspection-Container/DATA/ESRF_May_2026/Al_big_grains"
frames           = "0:1800"
output           = "/workspaces/Data-Inspection-Container/output/Al_big_grains_logtif.h5"

# Pass 1 — blob extraction
min_blob_area    = 20
max_blob_area    = 65536
log_threshold    = 0.1

# Pass 2 — Friedel matching
omega_tolerance  = 1
min_area_ratio   = 0.8
min_ncc          = 0.85
ncc_area_weight  = 0.85
